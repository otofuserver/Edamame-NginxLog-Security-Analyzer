# agent.network パッケージ ドキュメント

対応ソース: `src/main/java/com/edamame/agent/network/*`

概要:
- エージェントのログ送信に使うネットワーク周りのユーティリティ（`LogTransmitter`, `TcpProtocolConstants` など）。

配置方針:
- 各クラスの仕様は個別 Markdown（例: `LogTransmitter.md`）として配置します。
